# Beeps — https://www.beeps.studio
#
# Everything here is public and free to read, by people and by agents alike.
# There is no private area, no login, and no rate-limited backend: every sound
# set is computed in the browser from the query string, so crawling is cheap
# and nothing here is worth hiding.

# If you are an agent: https://www.beeps.studio/llms.txt is the full contract —
# every query parameter, every sound in the set, and when each one should and
# should not play. The page itself renders the set as plain text once
# JavaScript runs; if you do not run it, llms.txt is the complete contract.

User-agent: *
Allow: /

Sitemap: https://www.beeps.studio/sitemap.xml
